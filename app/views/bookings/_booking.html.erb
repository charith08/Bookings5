
<table>
  <thead>
    <tr>
      <th>By user</th>
      <th>Meeting name </th>
      <th>Start time</th>
      <th>End time</th>
      <th colspan="3"></th>
    </tr>
  </thead>

   </tr>
    <td><%= get_user_name(booking.user_id) %></td>
    <td><%= booking.name %></td>
    <td><%= readable_time(booking.start_time) %></td>
    <td><%= readable_time(booking.end_time) %></td>
    <% if is_admin? %>
    <% else %>
    <td><%= link_to "Add Participants", adduser_path(:room_id => @room.id, :booking_id => booking.id) %></td>
    <td><%= link_to "Display Participants", show_booking_users_path(:room_id => @room.id, :booking_id => booking.id) %></td>
    <% end %>
  </tr>

  </table>
