<p id="notice"><%= notice %></p>
<%= stylesheet_link_tag "/custom.css" %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>
<br>


<% if is_admin? %>
<% else %>
<% if  (@user.pusers.count  === 0) %>
<% else %>
  <h4>Meetings you've been added to</h4>
  <%= render @user.pusers %>
<% end %>
<% end %>
